import train_and_save as my

b = my.test('however, when a component needs to use another component in order to function, it adopts a used interface that specifies the services that it needs.')
print(b)
if b[1] > 90:
    

